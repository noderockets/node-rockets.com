---
name: 5 Software
route: /js-conf/software
menu: JSConf 2019
---

# Software

The rocket source code running on the raspberry pi can be [found here](https://github.com/noderockets/rocket).

## Edit Code
To edit the code directly on your pi, you'll need to ssh into your pi.

1. Open Terminal or a bash window
2. `$ ssh pi@10.0.0.84` (or whatever your sd card number is as the last number)
3. password is `raspberry`
4. `cd rocket`
5. All the code is here
6. `sudo node rocket` to run the server
7. Go to `http://10.0.0.84`(or whatever your sd card number is as the last number) in a browser to run the mission control dashboard
